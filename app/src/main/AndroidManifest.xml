<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.yesferal.hornsapp.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".presentation.HornsApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/HornsApp.AppTheme"
        tools:ignore="GoogleAppIndexingWarning,LockedOrientationActivity">

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${AD_MOB_APP_ID}" />

        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="${crashlyticsCollectionEnabled}" />

        <activity
            android:name=".presentation.ui.SplashActivity"
            android:theme="@style/HornsApp.Splash"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.yesferal.hornsapp.app.presentation.ui.concert.detail.ConcertActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name="com.yesferal.hornsapp.app.presentation.ui.home.MainActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>